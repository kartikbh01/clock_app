// Tabs
const stopwatchTab = document.querySelector(
  "#stopwatch-tab"
) as HTMLButtonElement;

const timerTab = document.querySelector(
  "#timer-tab"
) as HTMLButtonElement;

const stopwatchSection = document.querySelector(
  "#stopwatch-section"
) as HTMLElement;

const timerSection = document.querySelector(
  "#timer-section"
) as HTMLElement;

// Stopwatch
const stopwatchDisplay = document.querySelector(
  "#stopwatch-display"
) as HTMLDivElement;

const swStartBtn = document.querySelector(
  "#sw-start"
) as HTMLButtonElement;

const swPauseBtn = document.querySelector(
  "#sw-pause"
) as HTMLButtonElement;

const swResetBtn = document.querySelector(
  "#sw-reset"
) as HTMLButtonElement;

// Timer
const minutesInput = document.querySelector(
  "#minutes-input"
) as HTMLInputElement;

const secondsInput = document.querySelector(
  "#seconds-input"
) as HTMLInputElement;

const timerDisplay = document.querySelector(
  "#timer-display"
) as HTMLDivElement;

const timerStartBtn = document.querySelector(
  "#timer-start"
) as HTMLButtonElement;

const timerPauseBtn = document.querySelector(
  "#timer-pause"
) as HTMLButtonElement;

const timerResetBtn = document.querySelector(
  "#timer-reset"
) as HTMLButtonElement;


// features to implement
// 1. Tab switching
// 2. Stopwatch start
// 3. Stopwatch pause
// 4. Stopwatch reset
// 5. Timer start
// 6. Timer pause
// 7. Timer reset
// 8. Update displays
// 9. Input validation